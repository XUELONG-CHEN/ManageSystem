<?xml version="1.0" encoding="UTF-8"?>
  <web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
           xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
           xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
           http://xmlns.jcp.org/xml/ns/javaee/web-app_3_1.xsd"
           version="3.1">
  <display-name>ManageSystem</display-name>
  <welcome-file-list>
    <welcome-file>/pages/login.jsp</welcome-file>
  </welcome-file-list>
    <servlet>
        <servlet-name>springmvc</servlet-name>
        <servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
        <init-param>
            <param-name>contextConfigLocation</param-name>
            <param-value>classpath:springmvc/springmvc.xml</param-value>
        </init-param>
        <load-on-startup>1</load-on-startup>
    </servlet>
  <servlet-mapping>
    <!-- 与上面servlet-name中的值相对应，将*.do的请求指到DispatcherServlet中 -->
    <servlet-name>springmvc</servlet-name>
    <!-- *.do可以换成别的，与form表单中的action指向的请求相对应 -->
    <url-pattern>*.do</url-pattern>
  </servlet-mapping>
  <!--调用第三方包，进行过滤  -->
  <filter>
    <filter-name>CharacterFilter</filter-name>
    <filter-class>org.springframework.web.filter.CharacterEncodingFilter</filter-class>
    <init-param>
      <param-name>encoding</param-name>
      <param-value>UTF-8</param-value>
    </init-param>
  </filter>
  <filter-mapping>
    <filter-name>CharacterFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

  <context-param>
    <!--contextConfigLocation这个名字是spring已经定义好的，固定值 -->
    <param-name>contextConfigLocation</param-name>

    <!-- classpath指定了路径，则会去加载相应的xml,而不会去加载/WEB-INF/下的applicationContext.xml。
        如果没有指定的话，默认会去/WEB-INF/下加载applicationContext.xml。 -->
    <param-value>classpath:spring/applicationContext.xml</param-value>
  </context-param>
</web-app>
